const findElement = (node) => {
  return document.querySelector(node);
};

const findElements = (node) => {
  return document.querySelectorAll(node);
};

const API = 'https://pressa-events-server.herokuapp.com'

